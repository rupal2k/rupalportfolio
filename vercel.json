{
  "framework": "hugo",
  "buildCommand": "pnpm install --no-frozen-lockfile && hugo --gc --minify",
  "outputDirectory": "public",
  "build": {
    "env": {
      "HUGO_VERSION": "0.161.1",
      "HUGO_ENVIRONMENT": "production"
    }
  }
}
